import PageContainer from "../../components/PageContainer";
import Settings from '../../components/settings';

export default function PreferencePage({}) {
    return (
        <PageContainer title="Preferences Page">
            <Settings />
        </PageContainer>
    );
}